#include<bits/stdc++.h>
#define MAX 10000
using namespace std;
class Solution{
  public:
    int maxHist(vector<int> row) { // returns the max area of histogram


    }
    int maxArea(int M[MAX][MAX], int n, int m) {
      vector<row> r; 
    }
};

int main() {
  int r, c;
  cin >> r >> c;
  int arr[MAX][MAX];
  for(int i =0 ; i < r; i++) {
    for(int j= 0; j < c; j++) {
      cin >> arr[i][j];
    }
  }
  Solution s;
  cout << s.maxArea(arr, r, c);
  return 0;

}
